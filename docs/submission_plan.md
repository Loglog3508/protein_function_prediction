# 提交顺序与人工回填

## 推荐顺序

1. `submission_EXP-20260922-019.csv`：全部 113,796 条训练数据重训的低正则 3-5-mer SGD，使用阶段 6 固化的逐标签收缩阈值。
2. 阶段 5 稳健备选：`alpha=1e-4` 的 3-5-mer SGD 与其收缩阈值，仅在首提交异常或需要验证本地/线上相关性时生成。
3. 统一阈值变体：低正则模型统一阈值 0.65，仅在平台允许额外提交且逐标签阈值线上表现异常时使用。

CNN 和标签共现融合的本地交叉拟合分数均低于主模型，不建议占用每日提交机会。

## 上传前检查

- 文件应为 28,450 行、501 列。
- 第一列为 `protein_id`，其后严格为 `label_0` 至 `label_499`。
- ID 与 `data/test.csv` 顺序一致，无重复或缺失。
- 标签列为整数 0/1，无空值。
- 核对竞赛平台显示的每日次数、截止时间、文件大小和命名要求。

## 平台结果回填

| 提交文件 | 上传时间 | 平台是否解析 | 线上 Macro F1 | 备注 |
| --- | --- | --- | ---: | --- |
| `submission_EXP-20260922-019.csv` | 待填写 | 待填写 | 待填写 | 首提交 |

平台返回结果后，应同步更新 `artifacts/metrics/leaderboard.csv`、执行计划和最终报告中的线上结果。
