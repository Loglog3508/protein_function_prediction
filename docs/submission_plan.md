# 提交顺序与人工回填

## 推荐顺序

1. `submission_EXP-20260923-026.csv`：第一次提分迭代主模型。120,000 维 3-5-mer TF-IDF、balanced SGD、`alpha=5e-6`，本地双向交叉拟合 Macro F1 为 0.379956。
2. `submission_EXP-20260922-019.csv`：旧 30,000 维、`alpha=5e-5` 主模型，本地双向交叉拟合 Macro F1 为 0.317516。用于验证本地/线上排序相关性。
3. 新模型统一阈值 0.60 变体：仅在平台允许额外提交且逐标签阈值线上表现异常时生成。本地统一阈值 Macro F1 为 0.370108。

CNN 和标签共现融合的本地交叉拟合分数均低于主模型，不建议占用每日提交机会。

## 上传前检查

- 文件应为 28,450 行、501 列。
- 第一列为 `protein_id`，其后严格为 `label_0` 至 `label_499`。
- ID 与 `data/test.csv` 顺序一致，无重复或缺失。
- 标签列为整数 0/1，无空值。
- 核对竞赛平台显示的每日次数、截止时间、文件大小和命名要求。

## 平台结果回填

| 提交文件 | 上传时间 | 平台是否解析 | 线上 Macro F1 | 备注 |
| --- | --- | --- | ---: | --- |
| `submission_EXP-20260923-026.csv` | 待填写 | 待填写 | 待填写 | 第一次迭代首选 |
| `submission_EXP-20260922-019.csv` | 待填写 | 待填写 | 待填写 | 旧模型对照 |

平台返回结果后，应同步更新 `artifacts/metrics/leaderboard.csv`、执行计划和最终报告中的线上结果。
